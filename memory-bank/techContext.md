# Tech Context

## Technologies Used
- **Frontend Framework**: React 19
- **Build Tool**: Vite 7
- **Language**: TypeScript
- **Styling**: Tailwind CSS 4, Lucide React (icons)
- **UI Components**: Radix UI
- **Backend Services**: Firebase (Auth, likely Firestore/Storage)
- **Utilities**:
    - `axios`: API communication
    - `date-fns`: Date manipulation
    - `react-markdown`, `katex`: Markdown and math rendering
    - `react-pdf`: Document viewing
    - `react-dnd`: Drag and drop functionality
    - `react-router-dom`: Routing

## Development Environment
- **OS**: Windows 11
- **Node.js**: Likely required for npm/vite (v18+ recommended)
- **Package Manager**: npm (based on `package.json`)

## External Dependencies
- Firebase project setup required.
- API backend (URL not directly visible, but handled in `src/lib/api.ts`).

## Constraints
- Browser-based application.
- Responsive design for different device types.
